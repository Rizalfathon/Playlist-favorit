#include <iostream>

#include<cstring>

using namespace std;

Fint sparse_search(string a[], int n, string key) {

// sparse search

int s=0;

int en-1;

while (s<=e) (

int mid (s+e)/2;

int mid left = mid 1;

int mid right mid + 1;

if (a [mid]=="") {

while (1) {

if(mid_left<s and mid_right>e) (

return -1;

}

else if (mid_right<=e and a[mid_right]!="") {

mid mid right;

break;

}

else if (mid_left>=s and a[mid left]!=""){

mid mid left;

break;

}

mid left--;

mid_right++;

if (a [mid]==key) (

return mid;

else if (a[mid] > key) (

emid 1;

else

73

白

白

白

smid + 1;

return -1;  int main()

cout<<"masukkan data pencarian 1"<<endl;

string err("al", "robot", "love", "bat", "bird", "blue", "cat", "cat", "big", "color", "dog", "music"js

int n12

string fr cin>>f

cout<<sparse search (arr, n, f)<<endl

return 0
